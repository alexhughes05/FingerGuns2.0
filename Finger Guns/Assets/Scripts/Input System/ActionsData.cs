using UnityEngine;

public class ActionsData : MonoBehaviour
{

    private void DetermineIfPlayerFalling()
    {
        //if (fgmRb2d.velocity.y < 0 && !PlayerCurrentlyFlipping)
        //{
        //    Debug.Log("Player is falling.");
        //    fgmAnim.SetTrigger(FGMAnimHashes.PlayerFallingHash);
        //}
    }
    private void DetermineIfPlayerLanded()
    {
        ////Debug.Log("Player is grounded is " + player.PlayerGrounded + ". Player was grounded is " + player.PlayerWasGrounded);
        //if (true)//player.PlayerGrounded && player.PlayerWasGrounded)
        //{
        //    Debug.Log("Player has landed.");
        //    //player.PlayerWasGrounded = false;
        //    fgmAnim.SetTrigger(FGMAnimHashes.PlayerLandingHash);
        //}
    }
}


